<aura:component>
    <aura:attribute name="SquadList" type="QCSquadWrapper[]"/>
    
    <aura:iteration items="{!v.SquadList}" var="SquadWrap">
        <lightning:button>Hi, this is squad {!SquadWrap.squad.Name}</lightning:button>
    </aura:iteration>
</aura:component>